# 작업 로그

서버에 수행한 작업 내역을 최신이 위로 오게 기록한다.

## 2026-07-29 — 매매 데스크 분리 · 수동 청산 결함 · 숏은 인버스로 나간다

수행 주체: 클라우드 Claude 세션 (사용자 지시, PR #175·#176)

배포: `ce397e3 → a000d5c`. **장중 12:54 에 배포했다** — 일일 손실 한도 도달로 거래가
불가능하고 열린 포지션이 0건이라 사용자가 앞당길 것을 지시했다. trading·dash 둘 다.

### 결정

**1. 발굴엔진과 매매 데스크를 분리한다 — 같은 프로세스, 루프·예산만 (#175)**

실측: 신호 엔진 사이클이 **40~50초**였다(설정 `scan_interval_sec: 20`). 매매 tier 44종목 ×
분봉 백필 1콜 = 사이클당 45콜, 그 버스트에만 17~19초. **매매 tier 종목 수 ≈ 사이클 시간**이다.
발굴이 매매를 호출하는 게 아니라 **하나의 키움 예산을 6개 루프가 선착순으로 나눠 쓴다.**

착수 전에 확정한 사실이 설계를 바꿨다 — **가격은 이미 실시간이다.** `BarAggregator` 가 WS
틱으로 메모리에서 분봉을 만들고 `snapshot()` 은 콜이 0이다. **30초였던 것은 가격이 아니라
판정 주기다.** 그래서 "보유 5건을 1~3초마다 호출"이 아니라 **"1~3초마다 판정하고 WS 값이
낡았을 때만 REST 로 보충"** 이 맞다. 같은 목적을 추가 콜 0으로 이룬다.

`enabled: false` 로 배포했다. 실거래 청산을 다루므로 배포와 활성화를 분리한다.

**2. 손절·익절 라인 갱신은 훅 자리만 만든다 (#175)**

사용자가 "구조가 반영되면 룰을 따로 준다"고 명시했다. `update_lines()` 는 항상 '변화 없음'을
돌려주므로 켜져 있어도 매매 동작이 지금과 같다. 원본 `stop`/`target` 은 보존하고
`stop_live`/`target_live` 를 따로 둔다 — **갱신 규칙이 나빴을 때 되돌릴 기준**이 남아야 하고
일지·백테스트가 "원래 설계는 무엇이었나"를 계속 읽어야 한다.

**3. 수동 청산 버튼이 매도 주문을 내지 않았다 (#175)**

`POST /api/positions/{id}/close` 가 `orders.execute_exit` 을 부르지 않고 원장만 닫았다.
docstring 은 "장부상"이라 적어뒀지만 **화면 버튼은 그렇게 보이지 않는다** — 사용자는 청산했다고
믿고 계좌에는 남는다. 그것이 곧 고아 종목이다. 실제 시장가 매도를 발주하고 실패하면 원장을
닫지 않게 고쳤다. 계좌와 어긋난 항목을 장부에서만 지우는 `/void` 를 따로 냈다.

**의미가 다른 두 동작이 한 버튼에 섞여 있던 것이 원인이다.** 화면도 청산/제외로 나눴다.

**4. 실현손익을 증권사 라이브 값으로 (#175)**

7/28 실측: 대사 전 −12,238원 → 대사 후 −10,442원. **모델 비용이 손실을 1,796원 과대계상했고
일일 손실 가드가 그 값으로 판정했다.** 일지는 집계 전에 `reconcile` 을 1회 돌리고(실패해도
일지는 만들되 사실 목록에 남긴다), 화면은 `/api/account/realized` 의 당일 행을 주값으로 쓴다.
원장값·차이·실측 미확정 건수는 대조로 나란히 둔다.

**5. ★ 숏은 인버스 ETF 로 나간다 — 오전 진단이 틀렸다 (#176)**

오전에 '미체결 유령'으로 판정했던 2건을 void 하기 직전, 실계좌 체결내역과 대조해서 잡았다.

    ord_no 0185630 → KODEX 인버스(114800) 매수 11주 @1,235  09:32:19
    ord_no 0187803 → KODEX 인버스(114800) 매수  5주 @1,236  09:33:03

`orders.propose` 가 숏 신호를 `settings.INVERSE_ETF` 매수로 바꿔 발주한다(현물 계좌는 개별주
공매도 불가). **계좌에 034220·010140 이 없는 것이 정상이었다.** 두 곳이 이걸 몰랐다.

- `reap_unfilled`(그날 아침 배포한 코드)이 `symbol` 로 계좌를 대조해 **인버스로 나간 숏을
  항상 유령으로 읽는다** — 체결돼 살아 있는 포지션을 5분 뒤 무효 처리했을 것이다
- `reconcile_executions` 이 주문번호만 조인 키로 써서 ETF 체결가를 원 종목 진입가에 덮었다.
  LG디스플레이 9,020 → 1,235(슬리피지 −86.3%) · 삼성중공업 20,450 → 1,236(−94.0%)

덮인 값은 복구했고 핀텔에 실제 매도 주문번호(0393518)를 연결해 청산가를 1,302 → **1,299**
실측으로 맞췄다.

**교훈은 판정 순서에 있었다.** 계획서가 "실행 전 증권사 값과 대조해 확정하라"고 못박아
두지 않았으면 실체결 포지션 2건을 지웠다. `void` 가 삭제가 아니라 표시인 것도 같은 이유다.

### 미룬 것

- **매매 tier 상한 44 → 25**: 값은 확정했다(백필 종목당 ≈0.4초 실측 → `scan_interval_sec`
  20초의 절반을 백필에 쓰면 25). 다만 감시목록 크기가 shadow 강등 판정의 **입력**이라
  재측정 중에 바꾸면 7/30 판정이 흔들린다 → 판정 후 설정 한 줄
- **라인 갱신 룰**: 사용자 지침 대기
- **데스크 활성화**: 7/30 장 시작 후 30분 관찰

---

## 2026-07-28 — 실측가 게이트 · 본전 이동 shadow · 손실 한도 도달일 분석

수행 주체: 클라우드 Claude 세션 (사용자 지시, hosub MCP 읽기 전용 조회 + PR #174)

배포: `da97838(#170) → ce397e3` — #171·#172·#174 를 15:41:40 에 한 번에. 열린 포지션 0건 상태에서
재시작해 위험 없음. 다만 15:40 에 시작된 백테스트 리포트를 SIGTERM 으로 끊었고(재시작 후 자동 재실행).

### 결정

**1. 승격 판정에 실측 현재가를 쓴다 (#173)**

`_tradable()` 이 보는 가격이 소스마다 신선도가 달랐다. `nightly` 는 발굴 배치일 종가를, `news` 는
수집 시점 가격을 싣는다. 펌텍코리아 승격 결정의 '체결가능 48,600원' 은 7/27 종가였고 당시 현재가는
47,050원(−3.19%) — 그날 71번의 승격 판단이 전부 어제 가격으로 이뤄졌다. 수집 tier 후보에만
`ka10006` 을 불러 실측값으로 판정하고, 못 받으면 그 사이클 승격을 보류한다. 틀린 값으로 매매를
여는 것보다 30초 늦는 편이 싸다.

**2. 등락률은 판단에 넣지 않고, 체결강도는 기록만 한다 (#173)**

등락률은 이미 `filter_candidates`·`filter_gainers`·`filter_presurge` 세 군데서 쓴다 — 승격에서 또
보면 네 번째다. 체결강도는 어느 소스도 안 쓰는 새 정보지만 가격 모멘텀이 아니라 호가 불균형이라
축이 다르다. **좋은지 나쁜지 모른다** → `decisions` 에 적재만 하고 8/25 4주 측정에서 처음 묻는다.
테스트 2건이 이 규약을 고정한다.

**3. 청산 규칙을 바꾸기 전에 값부터 만든다 — 본전 이동 shadow (#174)**

출발은 "손절 종목 일부가 익절가 근처까지 갔다 되돌아왔다" 는 관찰이었다. 그런데 MFE 실측이 관찰을
축소했다 — 손절 10건 중 목표거리 50% 이상 도달은 **2건**, 8건은 13% 이하, 5건은 MFE 음수(진입가
위로 한 번도 못 감). 이틀 34건으로 넓혀도 손절 18건 중 5건이다.

그래서 **분봉 추세 분류기를 만들지 않기로 했다.** 발굴 3규칙 점수가 '그럴듯해서' 들어갔다가 1년 뒤
베타 프록시로 판명된 것과 같은 형태다. 대신 결정론 규칙 하나(목표거리 50% 도달 → 손절선을
진입가로)를 실제 청산과 나란히 돌려 20거래일 값을 만든다.

측정 설계에서 두 가지를 맞췄다.
- **봉 고가가 아니라 청산 감시와 같은 `price_of`·같은 30초 주기로 관측한다.** 봉 고가로 재면
  실시간 루프가 보지 못했을 값을 집계하게 된다 — 백테스트만 좋고 실전은 못 따라가는 함정이다.
- **손익은 모델 공식끼리 비교한다.** 원장은 #172 이후 실비용이라 그대로 빼면 비용 모델 차이가
  델타에 섞인다. 본전 이동은 손익 본전이 아니다 — 진입가에 팔아도 −0.2% 안팎 남는다.

관측 전용이다. `positions` 를 읽기만 하고 별도 테이블에만 쓴다. 끄면 기록만 멈추고 되돌릴 실거래
상태가 없다 — 아래 4번의 비대칭을 반복하지 않기 위한 설계다.

**4. 발굴 엔진 토글은 비대칭이다 — 켤 때는 즉시, 끌 때는 흔적이 남는다**

7/27 23:21~7/28 00:01 약 40분간 수동으로 `full` 로 열었다 닫았다(사용자 조작이며 shadow 가 샌 것이
아니다). 그 사이 15건이 실제 반영돼 감시목록에 `source='scout'` 13종목이 남았고, 그중 한국특강·웹젠이
매매 tier(`collect_only=0`)로 올라갔다. **웹젠은 7/28 실제로 매매돼 −1,653원**(그날 손실의 16%).

`mode='shadow'` 복귀는 이후 쓰기를 멈출 뿐 이미 쓴 행을 지우지 않는다. shadow 는 한국특강에 대해
강등 결정을 계속 내지만 `applied=0` 이라 되돌리지도 못한다. **사용자 지시로 둘 다 매매 tier 에
그대로 둔다.** 저절로 내려가지 않는다 — `collect` 모드도 `from_tier == TRADE` 를 건너뛰므로 `full`
로 열거나 수동으로 바꿔야 한다.

**5. 비용 모델 오차가 손실 가드를 실제보다 일찍 걸리게 하고 있었다 (#172)**

대사 후 7/28 실현손익 −12,238 → **−10,442원**(증권사 −10,620 과 178원 차이). 계좌 대비
−2.31% → **−1.97%**. 실비용은 수수료 10원 고정·세금 0.199% 로 붙었다.

11:49 가드 임시 해제를 판단할 때 화면값은 −2.3054% 였다. `extend` 는 **설정 한도(1.5%)에 더하는
것**이지 현재 손익에 더하는 게 아니라 새 한도가 −2.0%, 즉 해제한 순간 이미 초과였고 매매를 1건도
열지 못했다. 다만 실제값 −1.97% 기준이면 −2.0% 를 넘지 않아 재개됐을 수치다 — **결론(해제 무효)은
맞지만 원인 중 하나가 비용 모델 오차였다.**

9시대 성과도 부호가 뒤집혔다(−681 → **+773원**). 일지의 '시간대 손실 구간' 이 9·10·11시 →
10·11시로 줄었다.

**6. 재진입 쿨다운은 아직 넣지 않는다**

`ledger.open_symbols()` 는 **동시 보유만** 막는다. 고려산업은 11:09 청산 → 11:19 재진입으로 통과했고
두 건 합계 **−5,349원**, 그날 손실의 절반이다. 이틀간 재진입 4쌍이 있었다. 근거는 있으나 **표본이
이틀 34건**이라 보류하고, 본전 이동 shadow 측정과 함께 다시 본다.

**7. 오늘 손실의 주된 원인은 청산이 아니라 진입이다**

손절 10건 중 8건이 진입 직후 역행했고 5건은 진입가 위로 한 번도 못 갔다. **어떤 청산 로직도 이
8건은 못 살린다.** rsi_dip 이 이틀 연속 마이너스(10건 −14,874원)지만 10건이라 규칙 자체는 판단 보류.

### 측정 불가로 확인된 것

- **동서(026960) 는 MFE 분석이 안 된다** — 백필 실패가 아니라 유동성 부족이다. 오늘 339봉이 있지만
  보유 30분 구간(10:12~10:42)에 1분봉이 1개뿐이다. 16건 중 15건이 분석 상한이다.
- presurge 의 급증률 100,000% 초과 클램프는 마감 후 `signals: 0` 이라 검증하지 못했다.

### 남은 일

- [ ] 7/29 09:35 — scout 실측가 게이트 **장중** 동작 확인 (마감 후에는 `in_session=False` 라 시세
      호출 0건·승격 0건이 정상이고, 실제로 그렇게 나왔다)
- [ ] 7/29 09:35 — 심층 백필 재시도: 026960·175330 이 여전히 `done=7/27 · bars=0 · attempts=0` 로
      재시도 대상에 오르지 않았다. `attempts` 칸 자체는 생겼다
- [ ] 7/29 — presurge 클램프 장중 검증
- [ ] 7/27 실현손익 잔차 1,812원 — 실비용이 18건 중 0건이다(체결내역 조회 기간 밖)
- [ ] 본전 이동 shadow 20거래일 후 판정. **그전에 실제 청산 규칙으로 올리지 않는다** —
      테스트 3건(`shadow 가 실제 청산 판정을 바꾸지 않는다` 외)이 그 경계다
- [ ] 백테스트 리포트에서 거래세 0.15→0.20 영향과 그 외 원인 구분

## 2026-07-23 — trading 서비스 최초 배포 (런북: docs/requests/trading-deploy.md)

수행 주체: 클라우드 Claude 세션 (hosub MCP `run_command`/`write_file` 경유, 사용자 즉시 실행 지시)

- `/opt/hosub-trading` 에 `claude/confirmation-needed-2z6swl` 브랜치 클론 (hosub 사용자, 기존 원격 재사용)
- `trading/.venv` 생성 + 의존성 설치
- `trading/.env` 작성(권한 600): 실전 키, `DATA_DIR=/data/trading`, `INTERNAL_TOKEN` 발급
- `trading.service` systemd 등록·기동 — 초기 기동 실패 원인은 `.gitignore data/` 패턴이
  `app/data` 소스를 커밋에서 누락시킨 것. `/data/` 앵커링으로 수정 후 정상 기동
- **실계좌 키 검증(읽기 전용) 성공**: 토큰 발급 + 삼성전자 1분봉 900건 수신, 주문 API 미호출
- `/opt/hosub-mcp/.env` 에 `HOSUB_TRADING_URL`/`HOSUB_TRADING_TOKEN` 추가
  (대시보드 트레이딩 메뉴는 main 머지 + 자동 배포 후 활성화)
- 서비스 레지스트리(`config/registry.yaml`)에 trading 등록 — Git 경유(PR 리뷰 정책 준수)

남은 일:
- [ ] main 머지 → 5분 내 자동 배포로 대시보드 메뉴 활성화 확인
- [ ] 대시보드 API 설정 화면에서 계좌번호 입력
- [ ] 키움 포털에서 앱키 재발급(채팅 노출분 로테이션) 후 설정 화면에서 갱신
- [ ] 핸드오프 끝난 `local-request/trading-deploy` 브랜치 정리
